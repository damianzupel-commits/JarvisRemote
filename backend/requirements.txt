fastapi>=0.115
uvicorn[standard]>=0.30
openai>=1.40
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.7
playwright>=1.45
pyautogui>=0.9.54
pywinauto>=0.6.9
pywin32>=306; sys_platform == "win32"
comtypes>=1.4; sys_platform == "win32"
Pillow>=10.0
opencv-python-headless>=4.9
tree-sitter>=0.24
tree-sitter-language-pack>=0.11
python-frontmatter>=1.1
pathspec>=0.12
# Escáneres SAST reales para el centinela de seguridad de código (ver
# app/security/) -- Trivy (SCA) no viene por pip, se instala aparte.
semgrep>=1.171
bandit>=1.9
# cppcheck real para C/C++ (Semgrep --config auto casi no tiene reglas
# efectivas para ese stack, confirmado real auditando Luanti el 2026-07-29:
# 0 hallazgos, no por ausencia de bugs sino por ausencia de herramienta).
# Este wheel (mantenido por terceros, ver github.com/msclock/cppcheck-wheel)
# empaqueta el binario real de cppcheck -- a diferencia del cppcheck oficial
# (que en Windows solo distribuye un instalador .msi, sin build portable),
# esto no requiere admin/UAC ni instalación aparte.
cppcheck>=2.17
# clang-tidy real (analizador semántico de Clang -- dataflow interprocedural
# real vía clang-analyzer-*, más profundo que cppcheck) para C/C++. Este wheel
# empaqueta el binario real de LLVM, no un toolchain de compilación completo:
# SOLO produce hallazgos reales si el proyecto trae un compile_commands.json
# real (ver app/security/scanners.py::run_clang_tidy y
# find_compile_commands_dir) -- sin eso, clang-tidy (a diferencia de cppcheck,
# que trae headers sintéticos propios) ni siquiera puede parsear un #include
# de la librería estándar, confirmado real el 2026-07-30 en una máquina sin
# ningún compilador C/C++ instalado. `security_scan_project` lo salta con
# gracia (reportando el motivo) cuando no hay compilation database.
clang-tidy>=20
# Analizadores de calidad/bugs generales para Python (ver app/quality/) --
# ESLint/tsc (JS/TS) y detekt (Kotlin) no vienen por pip, se instalan aparte
# (requieren Node.js / JVM respectivamente).
ruff>=0.6
mypy>=1.11
# Firma real del log append-only del módulo de investigación (ver
# app/investigation/keys.py/log.py) -- ya estaba instalada como dependencia
# transitiva de otra cosa, pero es carga real para una pieza de seguridad,
# así que queda pinneada explícita en vez de confiar en que algo más la siga
# arrastrando.
cryptography>=42
# Centralidad de intermediación real para el halo del grafo de investigación
# (ver app/investigation/graph_metrics.py, spec sección 3: "nodo con alta
# centralidad de intermediación = candidato a pivote") y detección de
# comunidades (paso 7, Louvain) -- ninguna de las dos existía antes en el
# proyecto.
networkx>=3.3
# Parseo real de timestamps para la timeline forense (ver
# app/investigation/timeline.py, spec sección 4) -- datos reales de exports/
# logs vienen en formatos de fecha muy variados, `datetime.fromisoformat` de
# la stdlib es demasiado estricto (básicamente solo ISO 8601 exacto) para
# eso.
python-dateutil>=2.9
# Extracción de texto de PDF/DOCX para la pasada de NER (spec sección 2,
# paso 6: "Documentos PDF/DOCX -> extracción de texto para pasada de
# NER"). python-docx ya estaba instalada como dependencia transitiva de
# otra cosa (queda pinneada explícita, mismo criterio que cryptography más
# arriba); pypdf es nueva -- pura Python, sin binario externo que instalar
# aparte (a diferencia de cppcheck/clang-tidy más arriba, este caso no lo
# necesita).
pypdf>=5
python-docx>=1.2
# Export de informe del caso (spec sección 7, paso 8 -- último del orden de
# implementación): "grafo renderizado" es un requisito literal de la spec,
# no un nice-to-have -- matplotlib + el layout propio de networkx
# (spring_layout) generan una imagen PNG real del grafo del caso, embebida
# en el reporte. fpdf2 (pura Python, sin dependencia nativa tipo GTK/Pango
# que sí pide weasyprint en Windows) arma el PDF directo desde los mismos
# datos que el Markdown, sin necesitar un conversor markdown->PDF aparte.
matplotlib>=3.9
fpdf2>=2.7
